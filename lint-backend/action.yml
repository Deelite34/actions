name: lint-backend
description: lint the backend code using common defaults.

runs:
  using: composite
  steps:
    - uses: psf/black@22.12.0
      with:
        version: "22.12.0"
        src: backend
